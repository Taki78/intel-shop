Django==5.1.4
djangorestframework==3.15.2
djangorestframework-simplejwt==5.3.1
django-cors-headers==4.6.0
django-filter==24.3
Pillow==11.1.0
python-dotenv==1.0.1

# Pillow is used for admin image thumbnails — safe to skip if build fails on your Python version
